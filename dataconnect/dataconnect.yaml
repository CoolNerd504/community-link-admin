specVersion: "v1beta"
serviceId: "community-app"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "community-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
